from .interface import fetch, lazy_submit, submit
from .utils import (
    chunk,
    chunk_default,
    decode_text,
    extract_ints,
    frange,
    irange,
    iter,
    list,
    map,
    range,
    tail_call,
)
